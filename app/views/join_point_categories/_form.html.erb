<%= form_for(@join_point_category) do |f| %>
  <% if @join_point_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@join_point_category.errors.count, "error") %> prohibited this join_point_category from being saved:</h2>

      <ul>
      <% @join_point_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :point_id %><br />
    <%= f.number_field :point_id %>
  </div>
  <div class="field">
    <%= f.label :point_category_id %><br />
    <%= f.number_field :point_category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
