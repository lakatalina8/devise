<h1>Listing point_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @point_categories.each do |point_category| %>
  <tr>
    <td><%= point_category.name %></td>
    <td>&nbsp;<%= link_to 'Show', point_category %></td>
    <td><%= link_to 'Edit', edit_point_category_path(point_category) %></td>
    <td><%= link_to 'Destroy', point_category, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New PointCategory', new_point_category_path %>
