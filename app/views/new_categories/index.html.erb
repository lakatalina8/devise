<h1>Listing news categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @new_categories.each do |new_category| %>
      <tr>
        <td><%= new_category.name %></td>
        <td> &nbsp;<%= link_to 'Show', new_category %></td>
        <td><%= link_to 'Edit', edit_new_category_path(new_category) %></td>
        <td><%= link_to 'Destroy', new_category, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
  <% end %>
</table>

<br />

<%= link_to 'New NewsCategory', new_new_category_path %>

