<%= form_for(@tochka) do |f| %>
  <% if @tochka.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tochka.errors.count, "error") %> prohibited this tochka from being saved:</h2>

      <ul>
      <% @tochka.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
